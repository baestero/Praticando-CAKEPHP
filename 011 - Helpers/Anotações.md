# Helpers

- Por padrão o cake já carrega os helpers

- O paginator como não vem por padrão carregamos em AppView.php no initialize()

1. URL
2. Number
3. HTML
4. FORM

Em vendor/cakephp/cakephp/src/view/helpers conseguimos ver as classes dos helpers
